spring:
  cloud:
    config:
      enabled: false
  application:
    name: transaction-service
  datasource:
    url: jdbc:mysql://10.96.207.193:3306/transactions-service
    username: transactions-user
    password: transactions-pass
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
  sql:
    init:
      mode: always
